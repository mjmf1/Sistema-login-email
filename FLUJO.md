# 🔄 FLUJO DE LA APLICACIÓN

## 1️⃣ FLUJO DE AUTENTICACIÓN (LOGIN)

```
┌─────────────────────────────────────────────────────────────┐
│ USUARIO ACCEDE A http://localhost:3000/login                │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Página: /app/login/page.tsx                                 │
│ - Formulario con Email y Password                           │
│ - Estado local para email, password, error, loading         │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼ handleSubmit()
┌─────────────────────────────────────────────────────────────┐
│ POST → /api/auth/login                                      │
│ Body: { email, password }                                   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Endpoint: /app/api/auth/login/route.ts                      │
│ - Valida credenciales vs ADMIN_EMAIL y ADMIN_PASSWORD       │
│ - Si son correctas: Genera JWT con jwt.sign()               │
│ - Establece cookie HTTP-only con el token                   │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
        ▼ ✅ Exitoso                         ▼ ❌ Error
    token en cookie              Respuesta: { error: "..." }
        │                                    │
        ▼                                    ▼
  Redirect a               Mostrar error al usuario
  /dashboard              (mail o contraseña incorrectos)
        │                                    │
        └─────────────────┬──────────────────┘
                          │
                          ▼
            Actualizar UI / Mostrar resultado
```

---

## 2️⃣ PROTECCIÓN DE RUTAS (MIDDLEWARE)

```
┌─────────────────────────────────────────────────────────────┐
│ Usuario intenta acceder a una ruta                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Archivo: middleware.ts                                      │
│ - Se ejecuta en cada solicitud                              │
│ - Verifica si existe cookie 'auth_token'                    │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
   Token existe?                      No existe?
        │                                    │
        ▼ Sí                                ▼ No
    ¿Es /login?                     ¿Accede a /dashboard?
        │                                    │
    ┌───┴───┐                          Redirect a /login
    │       │
   Sí      No
    │       │
    ▼       ▼
Redirect Permitir acceso
a /dashboard a la ruta
    │       │
    └───┬───┘
        │
        ▼
  Usuario continúa o es redirigido
```

---

## 3️⃣ FLUJO DE ENVÍO DE EMAILS

```
┌─────────────────────────────────────────────────────────────┐
│ Usuario en /dashboard AUTENTICADO                           │
│ - Campo: Email destinatario                                 │
│ - Campo: Asunto                                             │
│ - Campo: Mensaje (textarea)                                 │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼ handleSubmit()
┌─────────────────────────────────────────────────────────────┐
│ POST → /api/email/send                                      │
│ Body: { to, subject, message }                              │
│ Cookie: auth_token (automática en el navegador)             │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ Endpoint: /app/api/email/send/route.ts                      │
│ - Obtiene token de las cookies                              │
│ - Verifica que el token sea válido                          │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
   Token válido?                      ¿Sin token?
        │                                    │
        ▼ Sí                                ▼ No
   Validar campos                      Respuesta: 401 (No autorizado)
   (to, subject, message)              El usuario va al login
        │                                    │
        ▼                                    └──────┐
   Todos los campos?                                │
        │                                           │
    ┌───┴────┐                                      │
   Sí       No                                      │
    │        │                                      │
    ▼        ▼                                      │
Continuar Error: Campo vacío                       │
    │        │                                      │
    ├────┬───┘                                      │
    │    │                                          │
    ▼    ▼                                          │
Llamar a sendEmail()   Mostrar error al usuario    │
(lib/email.ts)                │                     │
    │                         │                     │
    ├─────────────────────────┴─────────────────────┤
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ Función: sendEmail() en lib/email.ts                        │
│ - Crea transporter de Nodemailer                            │
│ - Configura SMTP (Gmail)                                    │
│ - Prepara email (from, to, subject, html)                   │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ transporter.sendMail() → SMTP Server (Gmail)                │
│ SMTP_HOST: smtp.gmail.com                                   │
│ SMTP_PORT: 587 (TLS)                                        │
│ SMTP_USER: tu_email@gmail.com                               │
│ SMTP_PASSWORD: contraseña de aplicación                     │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
   ✅ Email enviado                    ❌ Error al enviar
        │                                    │
        ▼                                    ▼
   Respuesta: 200                    Respuesta: 500
   { success: true,            { success: false,
     message: "..." }           message: "Error: ..." }
        │                                    │
        ▼                                    ▼
Mostrar "Email                  Mostrar error al usuario
enviado exitosamente"           (revisar credenciales SMTP)
        │                                    │
        └────────────────┬───────────────────┘
                         │
                         ▼
            Usuario ve el resultado en la UI
```

---

## 4️⃣ CIERRE DE SESIÓN (LOGOUT)

```
┌─────────────────────────────────────────────────────────────┐
│ Usuario en /dashboard hace clic en "Cerrar Sesión"          │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼ onClick()
┌─────────────────────────────────────────────────────────────┐
│ router.push('/login')                                       │
│ - Navega a la página de login                               │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│ middleware.ts verifica:                                     │
│ - ¿Ruta es /login?                                          │
│ - ¿Token en cookie?                                         │
└─────────────────────────┬───────────────────────────────────┘
                          │
        ┌─────────────────┴──────────────────┐
        │                                    │
   Token existe?                      Sí
        │
        ▼ La cookie sigue ahí
        
   El usuario puede hacer logout:
   1. Limpiar cookies (servidor)
   2. O simplemente navegar a /login
   3. La cookie expirará en 24 horas (maxAge)
        │
        ▼
   Usuario vuelve al formulario de login
```

---

## 5️⃣ ESTRUCTURA DE ARCHIVOS Y FLUJO

```
REQUEST INICIAL
     │
     ▼
middleware.ts ─────────► Verificar autenticación
     │
     ├─ ✅ Autenticado ──► Mostrar página solicitada
     │                    │
     │                    ├─ GET / ─────────► app/page.tsx (inicio)
     │                    │
     │                    ├─ GET /login ────► Redirigir a /dashboard
     │                    │
     │                    ├─ GET /dashboard ► app/dashboard/page.tsx (emails)
     │                    │
     │                    └─ POST /api/* ──► Procesar según endpoint
     │
     └─ ❌ No autenticado ──► Verificar ruta
                             │
                             ├─ GET /login ──► Permitir (formulario)
                             │
                             ├─ GET /dashboard ► Redirigir a /login
                             │
                             └─ POST /api/auth/login ► Procesar autenticación
```

---

## 📊 RESUMEN DE COMUNICACIÓN

### Frontend → Backend

| Acción | Método | Endpoint | Datos |
|--------|--------|----------|-------|
| Login | POST | `/api/auth/login` | `{ email, password }` |
| Verificar autenticación | GET | `/api/auth/verify` | (headers: cookies) |
| Enviar email | POST | `/api/email/send` | `{ to, subject, message }` |

### Backend → Frontend

| Endpoint | Respuesta Exitosa | Respuesta Error |
|----------|-------------------|-----------------|
| `/api/auth/login` | `200 + Cookie + JWT` | `401 + Error message` |
| `/api/auth/verify` | `200 + { valid: true }` | `401 + { valid: false }` |
| `/api/email/send` | `200 + Success message` | `400/500 + Error message` |

---

## 🔐 VARIABLES DE ENTORNO CRÍTICAS

```
Para autenticación:
├─ ADMIN_EMAIL
├─ ADMIN_PASSWORD
└─ JWT_SECRET

Para envío de emails:
├─ SMTP_HOST
├─ SMTP_PORT
├─ SMTP_USER
├─ SMTP_PASSWORD
└─ SMTP_FROM
```

---

**Cada parte del sistema está conectada y funciona de manera integrada para proporcionar autenticación segura y envío de emails funcional.**
